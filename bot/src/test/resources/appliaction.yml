app:
  telegram-token: ${TELEGRAM_TOKEN}
  scheduler:
    interval: 15
  github_base_url: localhost
  stackof_base_url: localhost

spring:
  application:
    name: bot
  jackson:
    time-zone: UTC

server:
  port: 8090

logging:
  config: classpath:log4j2-plain.xml

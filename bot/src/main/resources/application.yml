app:
  telegram-token: ${TELEGRAM_TOKEN}
  scrapper_base_url: http://localhost:8090/
  github_base_url: https://api.github.com
  stackof_base_url: https://api.stackexchange.com


spring:
  application:
    name: bot
  jackson:
    time-zone: UTC

server:
  port: 8080

logging:
  config: classpath:log4j2-plain.xml

springdoc:
  swagger-ui:
    path: /swagger-ui

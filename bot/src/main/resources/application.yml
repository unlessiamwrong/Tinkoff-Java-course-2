app:
  telegram-token: ${TELEGRAM_TOKEN}
  scrapper-base-url: http://localhost:8090/


spring:
  application:
    name: bot
  jackson:
    time-zone: UTC

server:
  port: 8080

logging:
  config: classpath:log4j2-plain.xml

springdoc:
  swagger-ui:
    path: /swagger-ui
